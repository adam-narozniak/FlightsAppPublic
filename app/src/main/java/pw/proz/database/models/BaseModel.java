package pw.proz.database.models;

/**
 * Interface for all model classes.
 *
 * It serves the purpose of enabling creation of generic Dao class that parameters are restricted to only the ones
 * implementing this interface (by extends keyword).
 */
public interface BaseModel {
}
